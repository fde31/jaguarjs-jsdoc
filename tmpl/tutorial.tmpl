<?js
var self = this;
if (packages && packages.length > 0) { ?>
    <?js= self.partial('metabar.tmpl', packages[0]) ?>
<?js } ?>
<section>

<header>
    <?js if (children.length > 0) { ?>
    <ul><?js
        children.forEach(function(t) { ?>
        <li><?js= self.tutoriallink(t.name) ?></li>
    <?js }); ?></ul>
    <?js } ?>

    <h2><?js= header ?></h2>
</header>

<article>
    <?js= content ?>
</article>

</section>
